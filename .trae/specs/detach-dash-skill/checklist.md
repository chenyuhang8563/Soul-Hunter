- [x] 检查项目中是否已成功添加 `detach` 动作且绑定到了 `R` 键。
- [x] 确认在被附身的非 Soldier 角色上，按住 `R` 键可以进入子弹时间（游戏变慢）。
- [x] 确认在按住 `R` 键期间，玩家无法控制当前角色移动。
- [x] 确认在按住 `R` 键期间，角色身上会出现一个长度约 40px 的像素箭头，且方向随玩家的方向键输入而变化。
- [x] 确认保持按住 `R` 键满 3 秒后，会自动触发脱离。
- [x] 确认松开 `R` 键时，会立即触发脱离。
- [x] 确认脱离触发后，游戏时间流速恢复正常。
- [x] 确认脱离触发后，当前的附身角色立即死亡。
- [x] 确认脱离触发后，原地生成了一个原初 Soldier，且玩家控制权已成功转移到该 Soldier 身上。
- [x] 确认新生成的 Soldier 会朝着箭头指示的方向执行一段 Dash 位移。
- [x] 确认原初的 Soldier 角色按下 `R` 键时，没有任何反应（不能套娃脱离）。
